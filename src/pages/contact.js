import React from 'react';
import Layout from '../components/Layout/layout';
const contact = props => {
    return (
        <Layout>
            <div></div>
        </Layout>
    );
}

export default contact;