import React from 'react';
import Layout from '../components/Layout/layout';
const blog = props => {
    return (
        <Layout>
            <div></div>
        </Layout>
    );
}

export default blog;